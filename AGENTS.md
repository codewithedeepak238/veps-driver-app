# Expo HAS CHANGED

This project targets **Expo SDK 54** (pinned to match the Expo Go app on the target device — Expo Go is locked to one SDK). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code, and do NOT bump the SDK without also updating Expo Go on the device.
